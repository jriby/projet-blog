<ul>
<% @post.comments.all.each do |comment| %>
    
    <p>Author : <%= comment.author %></p>
    <p>Comment : <%= comment.body %></p>
    <% if is_connected? %><p><%= link_to 'Destroy',comment_path(@post.id,comment.id), method: :delete %> </p><% end %>
  </li>
<% end %>
</ul>
